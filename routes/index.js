const { spotRoute } = require('./spot.route');
const { derivativeRoute } = require('./derivative.route');
const { farmingRoute } = require('./farming.route');
const { earnRoute } = require('./earn.route');
module.exports = {
    spotRoute,
    derivativeRoute,
    farmingRoute,
    earnRoute
}